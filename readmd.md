created a database , and connected it with cluster
npm init
src/App.js starting file for project
create server with the help of express.js
nodemon install so that we dont need to down the server and up the server

# app.use =>if use it , then the thing is, it will start matching starting routes, if match done , it will return that response. it will match all the http method api calls to /matching route

## Types of api

Get , post , put, patch, Delete, head, options

if we will make api calls , and not sending response, then it will get stuck in infinite loop

# multiple route handlers , play with it using next() keyword: bascially route handlers are the handle which handle request

Middleware is a function that has access to req, res, and next, and it can modify the request, response, or decide whether to continue.

# validator - for validationg email
